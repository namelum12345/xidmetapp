import 'package:flutter/material.dart';

import 'super_global_ban_screen.dart';

/// Parametrlərdən «Qlobal blok» üçün ekran (əsl məzmun [SuperGlobalBanScreen]-də).
class BanManagementScreen extends StatelessWidget {
  const BanManagementScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const SuperGlobalBanScreen();
  }
}
