import 'package:flutter/material.dart';

/// Design tokens for Qonşudan Xidmət.
abstract final class AppColors {
  static const Color background = Color(0xFFF8F9FB);
  static const Color primary = Color(0xFF6C63FF);
  static const Color onPrimary = Color(0xFFFFFFFF);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color outline = Color(0xFFE8EAEF);
  static const Color textPrimary = Color(0xFF1C1C1E);
  static const Color textSecondary = Color(0xFF6B7280);
  /// Skill-match badge (worker).
  static const Color matchBadge = Color(0xFF22C55E);
  static const Color onMatchBadge = Color(0xFFFFFFFF);
  /// Incoming chat bubble background.
  static const Color chatBubbleIncoming = Color(0xFFECEEF2);
}
